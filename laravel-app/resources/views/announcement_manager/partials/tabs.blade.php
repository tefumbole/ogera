{{-- Module tabs come from the layout's beyond-module-tabs bar (sidebar submenu).
     This partial only loads page styles so we do not render a second tab row. --}}
@include('announcement_manager.partials.styles')
