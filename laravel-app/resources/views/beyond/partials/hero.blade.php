<section class="ogera-page-hero">
    <div class="ogera-container">
        <h1>{!! $title !!}</h1>
        @if (!empty($subtitle))
            <p>{{ $subtitle }}</p>
        @endif
    </div>
</section>
